# Database Configuration

# MySQL Configuration
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=password
MYSQL_DATABASE=echo_chat

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017
MONGODB_DATABASE=echo_chat

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Server Configuration
PORT=8080
GIN_MODE=debug

#### Firebase Configuration ####
# Path to the Firebase service account key JSON file
GOOGLE_APPLICATION_CREDENTIALS=
PORT=8080

# Use the Firebase Auth Emulator for local development
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
# PORT=8080

# Catatan: Untuk Production (Cloud Run / VPS): Jika di Cloud Run, kamu tidak perlu menuliskan GOOGLE_APPLICATION_CREDENTIALS di .env. Cukup biarkan kosong, dan Firebase akan otomatis menggunakan Default Service Account yang menempel pada server tersebut. Ini jauh lebih aman.
